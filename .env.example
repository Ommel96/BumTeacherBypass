# Server port (default: 3847)
PORT=3847

# API keys (optional — can also be set in the Settings page)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# For Ollama: no key needed, just configure Base URL in Settings
# If running in Docker, set Base URL to http://host.docker.internal:11434